apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: single-service
  description: A simple test service component for basic integration tests
  tags:
    - test
    - service
spec:
  type: service
  lifecycle: experimental
  owner: user:default/test-user
